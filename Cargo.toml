[workspace]
members = [
  "stream",
  "hash",
  "endpoint",
  "protocol",
  "agent",
  "net",
  "discovery",
  "context",
  "log",
  "metrics",
  "ds",
  "tests",
]


[profile.release]
#opt-level = 3
#lto = "fat"
#panic = "abort"
#overflow-checks = false
#debug = false
#codegen-units = 1
