[workspace]
members = [
  "stream",
  "sharding",
  "endpoint",
  "protocol",
  "agent",
  "net",
  "discovery",
  "context",
  "log",
  "metrics",
  "ds",
  "rt",
  "tests",
]


[profile.release]
#opt-level = 3
#lto = "fat"
#panic = "abort"
#overflow-checks = false
#debug = false
#codegen-units = 1
#codegen-units = 1
#panic = "abort"
